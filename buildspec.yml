version: 0.2


phases:
  install:
    runtime-versions:
      standard: 2.0
  build:
    commands:
      - echo Nothing to do yet
artifacts:
  files:
    - assets/*
    - images/*
    - index.html
    - main.css
    - main.css.map
    - README.md
